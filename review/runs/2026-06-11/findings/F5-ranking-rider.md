# F5-ranking-rider — Findings (28)

| ID | Sev | Eff | Lane | file:line | Issue | Fix | Test idea |
|---|---|---|---|---|---|---|---|
| F5-DS-01 | P0 | M | B | `apps/web/app/(game)/league/[leagueId]/ranking/page.tsx:73-75, 348-350` | PostgREST 1000-row cap on unpaginated `rider_xp_daily` fetch. Both the... | Introduce a paginated helper (analogous to `_fetch... | Unit-test the ranking page data-fetch layer with a |
| F5-DS-02 | P1 | S | B | `apps/web/app/(game)/league/[leagueId]/achievements/actions.ts:37-39` | `equipAchievement` writes directly to the `teams` table via `supabase.... | Create an RPC `equip_achievement(p_league_id uuid,... | Write a vitest for `equipAchievement` that passes  |
| F5-DS-03 | P1 | S | A | `apps/web/app/(game)/league/[leagueId]/rider/[riderId]/rider-detail-client.tsx:791` | Raw `{r.xp_gained} XP` in `GameResultsSection` renders the XP integer ... | Replace `{r.xp_gained}` with `{formatThousands(r.x... | Snapshot test for `GameResultsSection` with a resu |
| F5-DS-04 | P1 | S | A | `apps/web/app/(game)/league/[leagueId]/rider/[riderId]/rider-detail-client.tsx:649` | The sticky CTA button uses `rounded-lg` (Tailwind default = 8px = `--r... | Change `className="rounded-lg px-4 py-1.5 ..."` at... | Visual regression snapshot of the sticky bar — ass |
| F5-D4 | P1 | S | B | `apps/web/app/(game)/league/[leagueId]/achievements/actions.ts:14-47` | `equipAchievement` validates only that the slug exists in `ACHIEVEMENT... | Recompute (or cache) the team's unlocked slug set ... | Call `equipAchievement` for a slug the team has no |
| PERF-01 | P1 | M | A | `apps/web/app/(game)/league/[leagueId]/ranking/page.tsx:73-76` | Unbounded `rider_xp_daily` fetch with no `.limit()` or `.range()`. At ... | Add `.limit(10000)` as an interim ceiling or imple... | Seed a league with 8 teams × 8 riders × 130 stage  |
| PERF-04 | P1 | M | A | `apps/web/app/(game)/league/[leagueId]/achievements/page.tsx:206-220` | Block 4 (Giro KOM + Points) issues one Supabase query per completed ye... | Collect all `finalSlug` values for all completed y... | Mock supabase with a spy. Call `AchievementsPage`  |
| F5-ARCH-01 | P1 | M | B | `apps/web/app/(game)/league/[leagueId]/ranking/page.tsx:73-76, 119-122, 347-351, 385-388` | Unpaginated `.select()` on `rider_xp_daily` (and `race_results` race-m... | Paginate the `rider_xp_daily` fetch (and any other... | Seed >1000 rider_xp_daily rows for one league, ass |
| F5-ARCH-03 | P1 | M | B | `apps/web/app/(game)/league/[leagueId]/achievements/page.tsx:166-221 (live) vs 364-411 (demo)` | The live and demo achievement-unlock logic have diverged in a correctn... | Collapse the two paths into one shared `computeAch... | With an in-progress (unfinished) Giro and the demo |
| F5-ARCH-04 | P1 | M | B | `apps/web/app/(game)/league/[leagueId]/achievements/page.tsx:94-98, 109-114, 236-240, 252-256, 297-301, 424-428, 439-443` | All achievement aggregation queries on `rider_xp_daily` and `race_resu... | Route every league-wide `rider_xp_daily`/`race_res... | Seed >1000 monument-eligible rider_xp_daily rows,  |
| F7-DS-006 | P1 | M | A | `apps/web/lib/achievements.ts:78-110` | Every entry in the `ACHIEVEMENTS` array carries an `accentColor` field... | Replace hex literals with DS semantic token refere... | A lint or snapshot test asserting no `ACHIEVEMENTS |
| F5-D1 | P1 | M | B | `apps/web/app/(game)/league/[leagueId]/achievements/page.tsx:172-187` | The GT jersey completion gate reads `gt_daily_classifications` for ALL... | Paginate the `allGiroStages` fetch (and the parall... | Seed >1000 `gt_daily_classifications` rows for a G |
| F5-D2 | P1 | M | B | `apps/web/app/(game)/league/[leagueId]/ranking/page.tsx:72-76` | The league-wide `rider_xp_daily` fetch (`.in("team_id", teamIds)`, all... | Add a paginated fetch helper (`.range()` loop) for... | Mock the supabase client to return exactly 1000 ro |
| F5-ARCH-02 | P1 | L | A | `apps/web/app/(game)/league/[leagueId]/ranking/page.tsx:13-317 vs 322-549` | The entire live ranking data pipeline (~230 lines: teams fetch, contra... | Extract a `fetchRankingData(supabase, leagueId, my... | Unit-test the extracted `fetchRankingData` once; a |
| PERF-03 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/ranking/page.tsx:276` | `teams.find((t) => t.id === c.team_id)` is called inside a loop over `... | Build `const teamNameById: Record<string, string> ... | Snapshot test: with 10 teams and 80 contracts, ver |
| PERF-07 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/ranking/page.tsx:119-129` | After the parallel batch completes, a sequential `await supabase.from(... | Hoist the race-meta fetch into the first `Promise.... | Assert that the network call to `race_results` for |
| F5-DS-05 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/achievements/achievements-client.tsx:21-23, 83-91` | Disabled filter chips (Budget, Roster) receive no accessible or visual... | Either: (a) pass a `disabled` prop through `Filter... | RTL test: render AchievementsClient, query the 'Bu |
| F5-DS-06 | P2 | S | B | `apps/web/app/(game)/league/[leagueId]/achievements/page.tsx:379-410` | The demo path for Block 4 (Giro KOM/Points) diverges from the live pat... | Align the demo Block 4 path with the live path: ap... | Unit test `renderDemoAchievements` with a mock Sup |
| F5-DS-07 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/rider/[riderId]/rider-detail-client.tsx:212` | Metric boxes in `renderMetrics()` use `rounded-lg` (8px, `--radius-lg`... | Replace `rounded-lg` with `rounded-[var(--radius-l... | Grep audit: ensure no bare Tailwind `rounded-lg` / |
| F5-ARCH-06 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/achievements/page.tsx:41-47, 138-153` | `monumentBase()` recompiles a `new RegExp(...)` for every MONUMENT_BAS... | Precompile the MONUMENT_BASES patterns once at mod... | Micro-benchmark / assert monumentBase returns iden |
| F5-ARCH-07 | P2 | S | B | `apps/web/app/(game)/league/[leagueId]/ranking/page.tsx:183-184, 230-231, 431-432` | Movement is computed by comparing today's vs yesterday's `team_ranking... | Introduce a shared `gameToday()/gameYesterday()` h... | Mock the clock to just-after-midnight UTC with a P |
| F6-08 | P2 | S | B | `apps/web/app/(game)/league/[leagueId]/achievements/actions.ts:37-42` | equipAchievement writes teams.equipped_achievement_slug directly via t... | Route through an equip_achievement RPC, or confirm... | As user A, attempt to set equipped_achievement_slu |
| TD-008 | P2 | S | B | `apps/web/app/(game)/league/[leagueId]/achievements/actions.ts:37-40` | equipAchievement validates that the slug exists in the client-side ACH... | Before the update, query the `team_achievements` t... | Call equipAchievement with a valid-but-unearned sl |
| SEC-F6-3 | P2 | S | B | `apps/web/app/(game)/league/[leagueId]/achievements/actions.ts:37-42` | equipAchievement validates the slug is a known achievement (line 19) a... | Compute (or query) the set of achievements the tea... | Vitest: call equipAchievement with a valid-but-une |
| PERF-06 | P2 | M | A | `apps/web/app/(game)/league/[leagueId]/achievements/page.tsx:93-154` | Block 1+2 (monument achievements) always fetches `rider_xp_daily` for ... | Consolidate: fetch all monument-relevant `rider_xp... | Confirm with a seeded league that the `rider_xp_da |
| F5-D5 | P2 | M | B | `apps/web/app/(game)/league/[leagueId]/achievements/page.tsx:236-265` | The Monument Man / Classic Man dynamic leaderboards aggregate `rider_x... | Paginate these two fetches with a `.range()` loop.... | Seed >1000 matching monument `rider_xp_daily` rows |
| F5-D6 | P2 | M | B | `apps/web/app/(game)/league/[leagueId]/achievements/page.tsx:378-411` | The demo achievements path (Block 4) does NOT use the shared `complete... | Make the demo path call the same `completedGrandTo... | Run both paths against a mid-Giro fixture (stage 1 |
| F7-11 | P2 | M | A | `apps/web/lib/achievements.ts:78-110` | Achievement definitions use hardcoded hex accent colors (`#f59e0b`, `#... | Replace hardcoded hex strings with CSS custom prop... | Grep for any hardcoded hex in the `accentColor` fi |