# B1-scoring — Findings (51)

| ID | Sev | Eff | Lane | file:line | Issue | Fix | Test idea |
|---|---|---|---|---|---|---|---|
| B1-01 | P0 | M | B | `services/pcs-sync/scoring.py:426-431` | The `prev_team_xp` idempotency baseline fetch (`rider_xp_daily` WHERE ... | Paginate this fetch with the project's `_fetch_all... | Mock supabase to return a >1000-row rider_xp_daily |
| PERF-01 | P0 | M | B | `services/pcs-sync/scoring.py:377-383, 426-428, 434-437, 499-501, 512-516, 529-531, 548-550, 569-574` | Eight unpaginated PostgREST fetches in calculate_daily_scores, none of... | Apply the _fetch_all pattern from goal_evaluator.p... | Seed >1000 race_results rows for a single race_slu |
| TD-01 | P0 | M | B | `services/pcs-sync/scoring.py:377-383` | race_results and rider_xp_daily fetches in scoring.py use bare .execut... | Replace the four bare-execute reads in calculate_d... | Add a test in test_pipeline_idempotence_guard.py m |
| PERF-04 | P0 | M | B | `services/pcs-sync/scoring.py:434-437` | Unpaginated `contracts` fetch (line 434-437). In a game with 10 teams ... | Apply the same `_fetch_all(query_factory, page_siz... | Write a test that mocks PostgREST returning exactl |
| DATA-01 | P0 | M | B | `services/pcs-sync/scoring.py:889-890, 902-904` | Idempotency hole: the rerun guard subtracts prev_team_xp from total_xp... | When prev_team_xp.get(team_id, 0.0) > 0 the team m... | Run calculate_daily_scores twice for a race_slug;  |
| DATA-02 | P0 | M | B | `services/pcs-sync/scoring.py:426-431, 499-501, 512-516, 529-531, 548-550, 569-574` | PostgREST 1000-row cap (a documented WH_CONTEXT failure mode): none of... | Wrap each unbounded GT-wide select in the existing... | Seed >1000 rider_xp_daily rows for a set of race_s |
| B1-01 | P0 | M | B | `services/pcs-sync/scoring.py:639-695` | Call the Bus tactic is dead code: it can never award XP. On a squad-ra... | When a `call_the_bus` activation exists for (team_... | Score a Giro stage where team T has an active call |
| TD-B1-01 | P0 | M | B | `services/pcs-sync/scoring.py:691-695` | call_the_bus tactic is dead code: bench riders (in_squad=False) are ej... | Move the call_the_bus handling BEFORE the `if not ... | test_call_the_bus_scores_bench_rider: set up a rid |
| SCORING-IDEMPOTENCY-DATE-FALLBACK | P1 | S | B | `services/pcs-sync/scoring.py:424-431, 902-904` | The XP double-count guard (delta_xp = total_xp - prev_team_xp) only lo... | Make the prev_team_xp prefetch run on both paths. ... | Call calculate_daily_scores twice with race_slugs= |
| PERF-04 | P1 | S | A | `services/pcs-sync/scoring.py:893-896, 921-922` | For each team with non-zero XP the code issues two sequential HTTP cal... | Replace the SELECT + UPDATE pattern with a single ... | Mock supabase.table('teams') and assert it is call |
| PERF-06 | P1 | S | B | `services/pcs-sync/tactics.py:84-86` | compute_nemesis_modifier uses bare assert statements for input validat... | Replace the three assert statements with explicit ... | Call compute_nemesis_modifier with outcome='unknow |
| TD-02 | P1 | S | B | `services/pcs-sync/scoring.py:424-431` | The delta-XP idempotency mechanism (prev_team_xp) is only activated wh... | Remove the `if race_slugs:` guard. Always fetch th... | Unit test: run calculate_daily_scores twice with t |
| PERF-03 | P1 | S | A | `services/pcs-sync/scoring.py:946-952` | One `team_ranking_daily` upsert per team row inside the snapshot loop ... | Collect all snapshot dicts for a league into a lis... | Assert that for a league with N teams, `team_ranki |
| PERF-05 | P1 | S | B | `services/pcs-sync/scoring.py:426-431` | Unpaginated `rider_xp_daily` idempotency pre-fetch (lines 426-431). Fo... | Paginate using `_fetch_all` or add `.limit(5000)` ... | Mock PostgREST to return 1001 xp_daily rows for a  |
| PERF-06 | P1 | S | B | `services/pcs-sync/scoring.py:529-534` | Unpaginated `gt_daily_classifications` fetch (lines 529-534). For a 21... | Apply `_fetch_all` pagination, identical to the fi... | Provide 1001 classification rows split across two  |
| DATA-03 | P1 | S | B | `services/pcs-sync/scoring.py:639-641, 691-695` | Dead-code correctness bug for the 'Call the Bus' tactic. compute_call_... | Allow call_the_bus to bypass the line-641 squad ga... | Activate call_the_bus for a team on a stage; give  |
| B1-02 | P1 | S | B | `services/pcs-sync/scoring.py:426-431` | Idempotency prev-XP fetch is unpaginated — exact PostgREST 1000-row-ca... | Paginate the prev fetch with a `_fetch_all`-style ... | Mock rider_xp_daily to return exactly 1000 prev ro |
| B1-03 | P1 | S | B | `services/pcs-sync/tactics.py:57-66` | compute_call_bus_modifier additionally gates on _is_stage_result (`/st... | Confirm the intended Call the Bus rule (bench ride... | Unit: compute_call_bus_modifier(False, 'race/giro- |
| TD-B1-03 | P1 | S | B | `services/pcs-sync/scoring.py:424-431` | Idempotency delta guard (prev_team_xp) is silently bypassed in today-d... | When race_slugs is None, derive the set of slugs f... | test_idempotency_today_mode: call calculate_daily_ |
| TD-B1-04 | P1 | S | B | `services/pcs-sync/scoring.py:486-497` | Single-slug cutoff applied to all slugs in a multi-slug call. Line 487... | Compute per-slug cutoffs: build a dict `cutoff_by_... | test_multi_date_slug_cutoff: pass two stage slugs  |
| TD-A-01 | P1 | S | A | `services/pcs-sync/tactics.py:84-86` | `assert` statements used for input validation in compute_nemesis_modif... | Replace all three asserts with explicit `if ... no... | test_compute_nemesis_invalid_outcome: pass outcome |
| SCORING-PAGINATION-1 | P1 | M | B | `services/pcs-sync/scoring.py:434, 426-428, 499-501, 529-531, 548-550` | scoring.py never uses db_utils._fetch_all (it doesn't even import it) ... | Import `_fetch_all` from db_utils and wrap every l... | Seed >1000 active contracts across 2+ leagues; run |
| B1-02 | P1 | M | B | `services/pcs-sync/scoring.py:377-379` | The main `race_results` fetch (and the GT fetches at 529-531 gt_daily_... | Route every GT-wide fetch in calculate_daily_score... | Mock race_results to return exactly 1000 rows for  |
| B1-03 | P1 | M | B | `services/pcs-sync/scoring.py:639-695` | The `call_the_bus` tactic is dead code on stage-race slugs. A bench ri... | Before the `if not in_squad: continue` gate, check... | Seed a non-squad contracted rider with pcs_points> |
| PERF-02 | P1 | M | A | `services/pcs-sync/scoring.py:557-562` | resolve_nemesis_for_stage is called once per stage slug in a serial Py... | If the RPC signature can be extended to accept an ... | Mock supabase.rpc and assert it is called exactly  |
| TD-03 | P1 | M | B | `services/pcs-sync/scoring.py:639-641 vs 691-694` | call_the_bus tactic is dead code for its intended case. The squad gate... | The squad gate must be relaxed for call_the_bus. O... | Test: a contracted rider NOT in the GT squad earns |
| PERF-01 | P1 | M | A | `services/pcs-sync/scoring.py:760-784, 814-830, 862-878` | One HTTP round-trip to Supabase per (rider, race_slug) upsert across t... | Accumulate all rider_xp_daily rows into a list dur... | Mock supabase and assert that `.upsert()` is calle |
| PERF-02 | P1 | M | A | `services/pcs-sync/scoring.py:892-922` | N+1 SELECT + UPDATE against the `teams` table: for every team_id that ... | Pre-fetch all teams in one query before the per-te... | Spy on supabase.table('teams').select and assert i |
| TD-B1-02 | P1 | M | B | `services/pcs-sync/scoring.py:434-437` | Unpaginated contracts fetch. All queries that call `.execute()` withou... | Implement a `_fetch_all(query_factory, page_size=1... | test_contracts_pagination: mock supabase to return |
| PERF-03 | P1 | L | A | `services/pcs-sync/scoring.py:760-784, 814-831, 861-878` | rider_xp_daily upserts are issued one-per-rider-per-race-slug inside t... | Accumulate upsert payloads into a list during each... | Mock supabase.table('rider_xp_daily').upsert and a |
| SCORING-BREAKAWAY-FLOAT | P2 | S | A | `services/pcs-sync/scoring.py:245` | _breakaway_distance_bonus returns `float(breakaway_kms) // BREAKAWAY_K... | Confirm whether the additive breakaway distance bo... | Unit-test _breakaway_distance_bonus(47) == 4 and a |
| SCORING-UNDERDOG-YOUTH-SLUG | P2 | S | A | `services/pcs-sync/scoring.py:280` | _underdog_multiplier excludes only `/gc`, `/points`, `/kom` from the f... | Add '/youth' to the tuple at line 280: `race_slug.... | assert _underdog_multiplier(272, 'race/giro-d-ital |
| B1-04 | P2 | S | B | `services/pcs-sync/scoring.py:280` | `_underdog_multiplier` zeroes the underdog boost on `/gc`, `/points`, ... | Add `/youth` to the endswith tuple at line 280 for... | Call `_underdog_multiplier(400, 'race/giro-d-itali |
| B1-05 | P2 | S | B | `services/pcs-sync/scoring.py:554-562` | `resolve_nemesis_for_stage` RPC is invoked once per stage inside scori... | Confirm `resolve_nemesis_for_stage` is idempotent ... | Run calculate_daily_scores twice on the same GT st |
| PERF-05 | P2 | S | A | `services/pcs-sync/scoring.py:946-952` | Step 5 (team_ranking_daily snapshot) issues one upsert per team per le... | Collect all team_ranking_daily dicts for a league ... | Mock supabase.table('team_ranking_daily').upsert a |
| PERF-07 | P2 | S | A | `services/pcs-sync/scoring.py:487` | When race_slugs contains stage-race slugs for multiple different dates... | Compute the cutoff per squad_slug inside the scori... | Pass race_slugs=['stage-10', 'stage-11'] where sta |
| PERF-08 | P2 | S | A | `services/pcs-sync/tactics.py:15` | BREAKAWAY_THRESHOLD_KM is defined in both tactics.py (line 15, value 3... | Define BREAKAWAY_THRESHOLD_KM once in a shared con... | Add a test that imports BREAKAWAY_THRESHOLD_KM fro |
| TD-04 | P2 | S | A | `services/pcs-sync/scoring.py:813-830 and 862-878` | The second pass (classif-only, lines 814-830) and third pass (final se... | Add `'underdog_mult': 1.0` to both the second-pass... | Verify the rider_xp_daily upsert dict for classif- |
| TD-05 | P2 | S | A | `services/pcs-sync/tactics.py:12-21 and scoring.py:61-62` | BREAKAWAY_THRESHOLD_KM (30.0) and the _in_breakaway helper are duplica... | Extract BREAKAWAY_THRESHOLD_KM and _in_breakaway i... | The existing test_breakaway_threshold_scoring_tact |
| TD-06 | P2 | S | A | `services/pcs-sync/scoring.py:477-478 and 486-487` | Phase ID and cutoff datetime are derived from squad_slugs[0] (the firs... | Add a validation guard: if len(set(_phase_year_fro... | Test: calling calculate_daily_scores with race_slu |
| TD-07 | P2 | S | A | `services/pcs-sync/scoring.py:760-784` | The rider_xp_daily upsert at lines 760-784 issues one HTTP round-trip ... | Collect all rider_xp_daily payloads into a list an... | Count the number of supabase.table(...).upsert cal |
| PERF-07 | P2 | S | A | `services/pcs-sync/scoring.py:639, 473, 483, 544` | `_is_squad_race(race_slug)` is called inside the innermost per-(rider,... | Memoize at the call site: build a `slug_is_squad: ... | Patch `_is_squad_race` with a call counter; assert |
| PERF-08 | P2 | S | A | `services/pcs-sync/scoring.py:799-801` | Linear contract scan in the classif-only second pass: `next(c for c in... | Build `contract_by_rider: dict[str, dict] = {c['ri... | Assert that replacing the linear scan with a dict  |
| DATA-04 | P2 | S | B | `services/pcs-sync/scoring.py:280` | Finals-guard inconsistency in _underdog_multiplier: it zeroes the boos... | Add 'youth' to the tuple at line 280: `race_slug.e... | Assert _underdog_multiplier(272, 'race/giro-d-ital |
| DATA-05 | P2 | S | B | `services/pcs-sync/scoring.py:418-419, 811, 947-951` | Date-stamping uses date.today() as the fallback and the team_ranking_d... | Derive the snapshot date from the stage's race_dat... | Score an old stage (race_date in the past) with ra |
| B1-04 | P2 | S | A | `services/pcs-sync/scoring.py:661,707,724,778` | tactic_applied is a single scalar overwritten by the last matching tac... | Either document that tactic_applied is best-effort... | Construct a stage where the same rider is both the |
| B1-05 | P2 | S | A | `services/pcs-sync/scoring.py:705,707-709,722,724-726` | A nemesis activation with outcome='no_resolution' still sets tactic_ap... | Only set tactic_applied when the nemesis actually ... | Score a stage with a nemesis_gc activation whose o |
| TD-A-02 | P2 | S | A | `services/pcs-sync/tactics.py:15` | BREAKAWAY_THRESHOLD_KM and _in_breakaway are duplicated verbatim betwe... | Extract BREAKAWAY_THRESHOLD_KM and _in_breakaway i... | test_breakaway_threshold_single_source: assert sco |
| TD-A-03 | P2 | S | A | `services/pcs-sync/scoring.py:769-781` | Duplicate column write: role_mult and gt_role_mult (and classif_bonus ... | Migrate: confirm no frontend consumer reads role_m... | Grep apps/web/lib and apps/web/app for 'role_mult' |
| SCORING-SNAPSHOT-TODAY | P2 | M | B | `services/pcs-sync/scoring.py:934-952` | The team_ranking_daily snapshot is always keyed on `today` (line 370, ... | Key the snapshot on the stage's race_date (race_da... | Score a stage with race_date in the past; assert a |
| SCORING-NEMESIS-CALL-BUS-ROLE | P2 | M | B | `services/pcs-sync/scoring.py:691-695, 805, 854` | call_the_bus includes bench (non-squad) riders for a stage (tactics.py... | Reorder so that when a call_the_bus tactic is acti... | Seed a contracted rider NOT in the GT squad, an ac |