# B3-auction-economy — Findings (32)

| ID | Sev | Eff | Lane | file:line | Issue | Fix | Test idea |
|---|---|---|---|---|---|---|---|
| TD-B3-06 | P0 | M | B | `services/pcs-sync/auction.py:239-242` | The per-rider exception handler at line 239 catches all exceptions and... | Wrap the (mark-bids + create-contract + update-rid... | Mock `contracts.insert` to raise; assert that afte |
| B3-02 | P1 | S | B | `services/pcs-sync/auction.py:189-207` | Resolution does NOT re-check the 7-day release cooldown that place_bid... | Before creating the contract (line 210), query `co... | Place a winning bid, then mark a released contract |
| TD-B3-01 | P1 | S | B | `services/pcs-sync/auction.py:148-157` | Winner/losers are marked 'won'/'outbid' BEFORE level-gating and duplic... | Defer marking any bid status until after all guard... | Add a test where the rider fails level-gating: ass |
| TD-B3-02 | P1 | S | A | `services/pcs-sync/auction.py:162` | `from sync import rank_max_for_level` is placed inside the hot per-rid... | Move the import to the module top level. Rename th... | Existing `test_rank_min_for_level` in test_sync_to |
| B3-01 | P1 | M | B | `services/pcs-sync/auction.py:92-272` | No optimistic lock / atomic claim before resolving. The Python resolve... | Claim each auction atomically before processing: i... | Simulate two concurrent resolve_current_round call |
| B3-AUC-01 | P1 | M | B | `services/pcs-sync/auction.py:189-219` | Existing-contract guard is not atomic with the contract INSERT, and it... | Make contract creation idempotent and atomic: move... | Seed one auction with a winning bid for a rider wh |
| B3-AUC-02 | P1 | M | B | `services/pcs-sync/auction.py:148-159, 209-224, 239-242` | Resolution is not crash-safe and not idempotent on rerun. The winner b... | Defer all status mutations until after the contrac... | Force the contract insert to fail for one rider mi |
| B3-AUC-03 | P1 | M | B | `services/pcs-sync/auction.py:159-187` | Resolution re-validates ONLY level gating, but place_bid (migration 20... | Re-run the same guards at resolution that place_bi... | Place a valid bid, then release the same rider (cr |
| TD-B3-03 | P1 | M | B | `services/pcs-sync/auction.py:110-111` | The fetch of active bids (`auction_bids.select(...).eq('auction_id', a... | Wrap the bids fetch in the project's standard `_fe... | Mock a scenario with exactly 1001 bids; assert tha |
| B3-01 | P1 | M | B | `services/pcs-sync/auction.py:110-112` | The active-bids fetch `supabase.table("auction_bids").select(...).eq("... | Paginate this fetch via the existing `_fetch_all` ... | Seed an auction with >1000 active bids and assert  |
| B3-02 | P1 | L | B | `services/pcs-sync/auction.py:148-219` | Resolution writes across four tables (auction_bids status, contracts i... | Move the winner-mark + contract-insert + rider-act... | Simulate a contract-insert failure (mock raising o |
| B3-04 | P2 | S | B | `services/pcs-sync/auction.py:205-227` | No re-validation that the winning bid still fits the team's purchasing... | Either accept that affordability is re-validated a... | Set a team treasury below its locked_salary + exis |
| B3-05 | P2 | S | B | `services/pcs-sync/auction.py:221-224` | `riders.is_active_in_game` is set to True globally on the riders table... | Confirm whether is_active_in_game is meant to be g... | Win a rider in league A, query league B's view of  |
| B3-06 | P2 | S | B | `services/pcs-sync/auction.py:159-178` | Min-salary floor is not re-checked at resolution. place_bid step 7 (re... | Decide whether the salary floor is frozen at bid t... | Place a bid at the floor, raise rider.monthly_sala |
| B3-07 | P2 | S | B | `services/pcs-sync/auction.py:108-272` | Per-rider and per-auction error handling swallows exceptions and conti... | Order mutations so the contract is created first (... | Force the contracts.insert to raise for one rider, |
| B3-08 | P2 | S | B | `services/pcs-sync/auction.py:285-303` | `_close_auction` opens the next 'scheduled' auction by `opens_at` orde... | Add `opens_at <= now()` (or a configurable gate) t... | Schedule a next auction with opens_at in the futur |
| B3-09 | P2 | S | A | `services/pcs-sync/auction.py:137-138` | Tiebreak sort `rbids.sort(key=lambda b: (-int(b['amount']), b['placed_... | Parse placed_at to a datetime (datetime.fromisofor... | Feed two equal-amount bids with placed_at strings  |
| B3-10 | P2 | S | A | `services/pcs-sync/auction.py:47-58` | `_get_current_phase_id` builds phase boundaries from the bid's `year =... | Make the fallback explicit: raise or return a sent... | Call _get_current_phase_id(date(2026,11,1)) and da |
| B3-AUC-04 | P2 | S | B | `services/pcs-sync/auction.py:47-58, 218` | _get_current_phase_id sets phase_recruited_id but mishandles dates out... | Make the phase calendar cover the full year (close... | Call _get_current_phase_id(date(2026,1,5)) and ass |
| B3-AUC-05 | P2 | S | A | `services/pcs-sync/auction.py:143-146, 163-171` | Three .single() calls (riders.full_name at 143, teams.level at 163, ri... | Replace .single() with .maybe_single() (or .limit(... | Resolve a winning bid whose team row was deleted;  |
| B3-AUC-06 | P2 | S | A | `services/pcs-sync/auction.py:306-336` | _cleanup_stale_drafts issues one DELETE per (team x contracted_rider) ... | Replace the nested loop with a single bulk delete:... | Seed N teams and M contracted riders with stale dr |
| TD-B3-07 | P2 | S | A | `services/pcs-sync/auction.py:93-96` | The expired-auctions query (`auctions.select('*').eq('status','open').... | Replace `select('*')` with `select('id, league_id,... | Code review / static check only — confirm no addit |
| TD-B3-08 | P2 | S | B | `services/pcs-sync/underdog.py:11-15` | `recompute_eligibility` returns `resp.data` directly without any error... | Add `if not resp.data or not resp.data.get('ok'): ... | Add a test where `sb.rpc(...).execute()` returns ` |
| TD-B3-09 | P2 | S | A | `services/pcs-sync/underdog.py:9` | `recompute_eligibility` is a synchronous function (`def`, not `async d... | Either make `recompute_eligibility` `async def` fo... | Static analysis / type check only — `mypy --strict |
| B3-04 | P2 | S | A | `services/pcs-sync/auction.py:143-146` | `riders.select("full_name").eq("id", rider_id).single()` (and the `.si... | Replace `.single()` with `.maybe_single()` (or bat... | Run resolution for a rider whose riders row was de |
| B3-05 | P2 | S | A | `services/pcs-sync/auction.py:47-58` | `_get_current_phase_id` iterates _PHASES for the given year and return... | Return None or raise (and have the caller skip/log... | Call `_get_current_phase_id(date(2026,12,1))` and  |
| B3-07 | P2 | S | A | `services/pcs-sync/underdog.py:11-15` | `recompute_eligibility` calls the RPC and returns `resp.data` with no ... | Check `resp.data` is the expected jsonb payload (e... | Mock the RPC to return `data=None` and assert reco |
| B3-03 | P2 | M | B | `services/pcs-sync/auction.py:62-96` | `force_close=True` (the `--close` CLI flag in resolve_now.py:28) drops... | Add an optional `league_id` filter to `resolve_cur... | With force_close=True and two leagues each having  |
| TD-B3-05 | P2 | M | A | `services/pcs-sync/auction.py:306-336` | `_cleanup_stale_drafts` issues one `DELETE` call per (team_id, rider_i... | Replace the nested loop with a single `DELETE FROM... | Assert `supabase.table('draft_bids').delete` is ca |
| B3-03 | P2 | M | B | `services/pcs-sync/auction.py:161-187` | Level gating reads the team's CURRENT level (`teams.select("level").eq... | Snapshot the team level (or the eligibility result... | Place a valid bid at level N, raise the team's lev |
| B3-06 | P2 | M | B | `services/pcs-sync/auction.py:17-18, 226-230` | Resolution explicitly does not re-validate that the winning team can a... | Re-validate aggregate affordability per team at re... | Construct a team whose treasury is below the sum o |
| TD-B3-04 | P2 | L | A | `services/pcs-sync/auction.py:163-168` | Level-gating fires two separate single-row DB queries per rider inside... | Batch-fetch team levels and rider pcs_ranks before... | Assert that `supabase.table('teams').select` is ca |