# B2-sponsor-goals — Findings (45)

| ID | Sev | Eff | Lane | file:line | Issue | Fix | Test idea |
|---|---|---|---|---|---|---|---|
| B2-01 | P0 | M | B | `services/pcs-sync/goal_evaluator.py:651-724` | Goal payout is non-idempotent and can permanently LOSE money on partia... | Make the completion insert + treasury credit atomi... | Simulate insert success + treasury UPDATE raising  |
| TD-03 | P0 | M | B | `services/pcs-sync/goal_evaluator.py:572-575` | Idempotency pre-fetch for `sponsor_goal_completions` is not paginated.... | Wrap with `_fetch_all(lambda: supabase.table('spon... | Mock `_fetch_all` to return 1001 rows and assert t |
| DATA-1 | P0 | M | B | `services/pcs-sync/goal_evaluator.py:674-724` | Goal completion insert and treasury credit are split into two separate... | Move the goal credit behind a single SECURITY DEFI... | Mock supabase so the completion insert succeeds bu |
| B2-01 | P0 | M | B | `services/pcs-sync/sponsor_bonus.py:346-397:346-397` | No-cumul rule (GAME_RULES §18) is NOT idempotent across the multi-stag... | After building the `neutralized` set, actively rev... | Credit a sprinter base bonus for stage-3, then com |
| B2-02 | P0 | M | B | `services/pcs-sync/goal_evaluator.py:674-724:674-724` | Goal payout is split across two non-atomic operations with separate tr... | Make the completion insert and the treasury credit... | Mock the teams.update to raise after a successful  |
| B2-02 | P1 | S | B | `services/pcs-sync/goal_evaluator.py:697-705` | Treasury credit uses a non-atomic read-modify-write: it SELECTs teams.... | Replace the SELECT-then-absolute-UPDATE with a rel... | Concurrency-style test: two credits interleaved (r |
| B2-03 | P1 | S | A | `services/pcs-sync/goal_evaluator.py:451-453` | team_sponsors is fetched with a bare `.execute()` (not _fetch_all), so... | Wrap the team_sponsors select in `_fetch_all(lambd... | Mock supabase to return 1000 rows on the first pag |
| TD-B2-02 | P1 | S | B | `services/pcs-sync/reconcile_bonuses.py:34-40` | `reconcile_team_treasury` ignores `sponsor_bonus_revert` treasury_log ... | Add `sponsor_bonus_revert` to the sum expression s... | Add a test case to `test_reconcile_bonuses.py`: te |
| DATA-4 | P1 | S | B | `services/pcs-sync/sponsor_bonus.py:481-493` | Same non-atomic read-modify-write as DATA-2, in the revert path: SELEC... | Use a relative-decrement RPC under FOR UPDATE. Rem... | Revert a bonus when treasury < final_bonus; assert |
| TD-02 | P1 | S | B | `services/pcs-sync/goal_evaluator.py:695-704` | Treasury credit uses a read-modify-write pattern (SELECT treasury → UP... | Extract treasury credit into the same `credit_spon... | Run evaluate_sponsor_goals twice concurrently agai |
| TD-04 | P1 | S | B | `services/pcs-sync/goal_evaluator.py:691-693` | When `insert sponsor_goal_completions` throws (line 692), the function... | Wrap both the INSERT and the treasury credit in a ... | Mock the treasury UPDATE to raise after INSERT suc |
| B2-04 | P1 | S | A | `services/pcs-sync/reconcile_bonuses.py:34-38:34-38` | reconcile_team_treasury only sums treasury_log rows of type 'sponsor_b... | Include 'sponsor_bonus_revert' (and any goal-rever... | make_supabase with a 'sponsor_bonus' +20000, a 'sp |
| B2-05 | P1 | S | B | `services/pcs-sync/goal_evaluator.py:534-536:534-536` | gt_final_classifications is fetched via `.eq('race_slug', f'{parent_sl... | Confirm gt_final_classifications grain. If league-... | Seed two leagues with the same parent_slug final c |
| B2-04 | P1 | M | B | `services/pcs-sync/goal_evaluator.py:451-471` | evaluate_sponsor_goals has no league scoping: it fetches ALL team_spon... | Scope the team_sponsors and squad/role fetches (an... | Seed two leagues with identical T4 sponsor + winni |
| B2-05 | P1 | M | B | `services/pcs-sync/goal_evaluator.py:281-291` | eval_two_riders_win_stage (CLM/SH 'two riders' goals) and eval_two_rid... | Evaluate the 'two riders' conditions per stage aga... | Two squad riders win stages but one was added to t |
| B2-01 | P1 | M | B | `services/pcs-sync/goal_evaluator.py:594-597` | all_squad_riders is seeded from the squad as of date.today() (line 595... | Drop the date.today() seed (line 595-597) entirely... | Unit test evaluate_sponsor_goals with two stage wi |
| B2-02 | P1 | M | B | `services/pcs-sync/reconcile_bonuses.py:34-38` | reconcile_team_treasury sums treasury_log only for types ('sponsor_bon... | Include 'sponsor_bonus_revert' in the type filter ... | Extend test_reconcile_bonuses with a team having a |
| B2-03 | P1 | M | B | `services/pcs-sync/goal_evaluator.py:690-724` | Non-atomic credit with no retry path on partial failure. The sponsor_g... | Make completion+credit atomic: move both the compl... | Mock the treasury UPDATE to raise after a successf |
| TD-B2-01 | P1 | M | B | `services/pcs-sync/goal_evaluator.py:695-705` | Read-modify-write treasury race condition in goal_evaluator. The treas... | Introduce a `credit_goal_reward` RPC (SECURITY DEF... | Mock supabase so `.select` returns treasury=100k a |
| DATA-2 | P1 | M | B | `services/pcs-sync/goal_evaluator.py:697-705` | Treasury is credited via a non-atomic read-modify-write: it SELECTs te... | Perform the credit through an RPC that does a rela... | Hard to unit-test concurrency directly; assert at  |
| DATA-3 | P1 | M | B | `services/pcs-sync/sponsor_bonus.py:475-516` | Step-7 stale-bonus revert deletes the sponsor_bonuses row (line 506-50... | Either route the revert through an idempotent RPC ... | Insert a GT-stage bonus for a rider not in squad a |
| TD-01 | P1 | M | A | `services/pcs-sync/goal_evaluator.py:451-453` | team_sponsors fetch is not paginated. The query at line 451 uses a bar... | Replace the bare `.execute()` call with `_fetch_al... | Seed 1001 team_sponsor rows in a mock and assert ` |
| B2-03 | P1 | M | B | `services/pcs-sync/goal_evaluator.py:696-705:696-705` | Goal treasury credit does a non-atomic read-modify-write on teams.trea... | Replace the direct SELECT-then-UPDATE with a relat... | Hard to unit-test the race directly; assert at min |
| B2-07 | P2 | S | A | `services/pcs-sync/reconcile_bonuses.py:31-38` | reconcile_team_treasury computes `expected` as the FULL sum of sponsor... | Include 'sponsor_bonus_revert' (and 'gt_goal_bonus... | Seed a team with a credited then reverted GT bonus |
| B2-08 | P2 | S | B | `services/pcs-sync/sponsor_bonus.py:352-367` | No-cumul neutralization keys are matched on (team_id, rider_id, slug) ... | Persist the neutralized decision on the sponsor re... | Run process_race_bonuses on stage slugs only, then |
| B2-05 | P2 | S | B | `services/pcs-sync/sponsor_bonus.py:354-364` | The no-cumul neutralized lookup derives parent_slugs via re.match(r'^(... | Add a regression test asserting that when a gc_pod... | Integration-style test: one completion row with ne |
| B2-06 | P2 | S | B | `services/pcs-sync/sponsor_bonus.py:480-503` | Stale-bonus revert does a non-atomic read-modify-write on teams.treasu... | Route the revert through an atomic relative-UPDATE... | Test that reverting a stale bonus when treasury <  |
| TD-B2-03 | P2 | S | A | `services/pcs-sync/goal_evaluator.py:451-453` | `team_sponsors` fetch is NOT paginated. At line 451-453 a bare `.execu... | Replace the `.execute()` call at line 453 with `_f... | Unit test: mock `_fetch_all` to return two pages o |
| TD-B2-04 | P2 | S | A | `services/pcs-sync/reconcile_bonuses.py:27-40` | Three `.execute()` calls in `reconcile_team_treasury` (`sponsor_bonuse... | Wrap each of the three inner `.execute()` calls wi... | Pass a mock that returns 1001 rows in two pages; a |
| TD-B2-05 | P2 | S | A | `services/pcs-sync/goal_evaluator.py:281-291` | `eval_two_riders_win_stage` is defined and registered in `EVALUATORS` ... | If the goal is not intended, remove `eval_two_ride... | Add an assertion test that every evaluator key in  |
| TD-B2-07 | P2 | S | A | `services/pcs-sync/sponsor_bonus.py:248-269` | `phase_ids` set is built (lines 250-261) but never used. The GT squad ... | Either remove the `phase_ids` set and its populati... | Linting/static analysis catches this as an unused  |
| DATA-6 | P2 | S | B | `services/pcs-sync/goal_evaluator.py:183-186, 249-258, 204-209` | Goal evaluators that pick a single triggering rider iterate over Pytho... | Sort eligible riders deterministically (e.g. by gc... | Two eligible riders both top-5 in GC; run eval twi |
| DATA-7 | P2 | S | B | `services/pcs-sync/sponsor_bonus.py:431-435` | The treasury_log description embeds the float multiplier `(×{multiplie... | Make the RPC idempotence key stable: dedup on (tea... | Credit a bonus, then rerun with a changed multipli |
| TD-06 | P2 | S | B | `services/pcs-sync/sponsor_bonus.py:248-269` | `phase_ids` is populated (lines 250-261) but never used. The GT squad ... | Add `.eq('phase_id', phase_id)` to the `gt_squad` ... | Set up two teams with riders in the Giro squad and |
| TD-07 | P2 | S | A | `services/pcs-sync/reconcile_bonuses.py:29-41` | `reconcile_team_treasury` uses bare `.execute()` for three separate pe... | Replace each bare `.execute()` with `_fetch_all(..... | Mock 1001 sponsor_bonus rows for a single team; as |
| TD-08 | P2 | S | A | `services/pcs-sync/reconcile_bonuses.py:35-39` | `reconcile_team_treasury` counts `treasury_log` rows with `type in ('s... | Add `'sponsor_bonus_revert'` to the type filter an... | Insert a sponsor_bonus row, a treasury_log credit, |
| TD-09 | P2 | S | A | `services/pcs-sync/goal_evaluator.py:103-109` | `_parse_ts` in `goal_evaluator.py` is a duplicate of the same function... | Move the canonical implementation to `db_utils.py`... | Write parametrized tests in `test_db_utils.py` cov |
| TD-10 | P2 | S | A | `services/pcs-sync/sponsor_bonus.py:480-503` | Step 7 stale-bonus revert (lines 467-516) applies to ALL GT stage slug... | Gate the Step 7 loop: only run it when at least on... | On a first-time run (no existing bonus rows), asse |
| B2-06 | P2 | S | B | `services/pcs-sync/goal_evaluator.py:595:595` | all_squad_riders is seeded with squad members at the cutoff for date.t... | Drop the date.today() seed (line 595-597) and rely... | Add a rider to the squad AFTER the GT's last stage |
| B2-07 | P2 | S | B | `services/pcs-sync/sponsor_bonus.py:409-436:409-436` | Within process_race_bonuses, upsert_rows always appends a row (lines 4... | Either freeze final_bonus on already-credited keys... | Pre-seed sponsor_bonuses with final_bonus 20000 fo |
| B2-06 | P2 | M | B | `services/pcs-sync/goal_evaluator.py:594-597` | all_squad_riders for the 'two riders' family includes `_squad_at_cutof... | Drop the date.today() squad snapshot entirely; bui... | Run goal eval for a past GT, then change current s |
| B2-04 | P2 | M | B | `services/pcs-sync/goal_evaluator.py:474-476` | race_results / classifications fetches use .like(f'{parent_slug}%') wi... | Anchor the pattern to the path separator: fetch wi... | Seed race_results for 'race/x/2026/stage-1' and a  |
| B2-07 | P2 | M | B | `services/pcs-sync/goal_evaluator.py:281-291` | eval_two_riders_win_stage counts winners against all_squad_riders (unt... | Replace all_squad_riders membership tests in eval_... | Two riders win stage-2 and stage-9; rider B was on |
| TD-B2-06 | P2 | M | A | `services/pcs-sync/sponsor_bonus.py:214-218` | Deferred imports and nested function definitions inside `process_race_... | Move `_parse_ts`, `_is_gt_stage`, and the ZoneInfo... | Delete the local definitions and verify the existi |
| DATA-8 | P2 | M | B | `services/pcs-sync/sponsor_bonus.py:352-367, 391-393` | The no-cumul neutralization set is keyed on (team_id, rider_id, slug) ... | When a goal completion is created (or via the stan... | Credit a base GC bonus, then run goal evaluation t |