# F4-auction-ui — Findings (54)

| ID | Sev | Eff | Lane | file:line | Issue | Fix | Test idea |
|---|---|---|---|---|---|---|---|
| TD-003 | P0 | S | B | `apps/web/app/(game)/league/[leagueId]/auction/market/actions.ts:78-101` | confirmPhaseSetup accepts an arbitrary `teamId` string from the caller... | Add auth + ownership verification before calling t... | Call confirmPhaseSetup with a teamId that does not |
| F4-02 | P0 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:504-543` | At round resolution forceResolveRound awards the highest bid and creat... | Before inserting the winning contract (step 7g), r... | Set team treasury just above one bid, place two wi |
| BR-01 | P0 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/actions.ts:76-79` | cancelBid mutates auction_bids directly via the anon client (UPDATE st... | Move bid cancellation into a SECURITY DEFINER RPC ... | Integration: as a team owner, call the PostgREST R |
| F4-05 | P1 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/page.tsx:89-92` | Round/UI labels are hardcoded to a 3-round model: currentRound is clam... | Remove the hardcoded /3 and the 3-round clamp; ren... | Render the page for a 4-round phase and assert the |
| PERF-02 | P1 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/page.tsx:279-286` | Conditional sequential query for `pendingSponsorName`: if `pendingSpon... | Include the sponsor name in the team select at lin... | Add an integration test that mocks a team with `pe |
| DS-06 | P1 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/treasury-widget.tsx:47` | The 'Available budget' value switches from `--success` (green) to `--s... | Add a three-state threshold: green (`available >= ... | Render TreasuryWidget with available=30000; assert |
| TD-004 | P1 | S | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:368-395` | forceResolveRound is callable by ANY league member, not just the commi... | After the membership check, additionally verify th... | Mock a non-commissioner user membership, call forc |
| F4-03 | P1 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:557-574` | forceResolveRound fetches contracts (step 8) with an unpaginated .sele... | Paginate both reads with a _fetch_all-style loop (... | Seed >1000 active bids spanning many riders in one |
| F4-04 | P1 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/page.tsx:87-97` | currentRound is derived from a day-count since opens_at: floor((now - ... | Derive the current round from auction/round record... | Configure a round whose window is 2 days and anoth |
| PERF-01 | P1 | M | A | `apps/web/app/(game)/league/[leagueId]/auction/page.tsx:127-135` | Sequential waterfall: rider XP query is blocked behind the `Promise.al... | Either (a) include `rider_xp_daily` in the initial... | Instrument the server component with `console.time |
| PERF-03 | P1 | M | A | `apps/web/app/(game)/league/[leagueId]/auction/page.tsx:265-273` | Conditional sequential query for `existingAuctionBids`: issued seriall... | Since `openRound` is derived from `auctionRounds` ... | Time the server render with and without an open ro |
| F4-ARCH-03 | P1 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/page.tsx:1-269` | Two competing, mutually-inconsistent auction-bidding models coexist. T... | Decide on one model. If draft+validate is canonica... | Routing/dead-code test: assert no Link or router.p |
| SEC-F6-1 | P1 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:368-414` | forceResolveRound is an exported "use server" action invoked directly ... | Move the resolution authority behind a guard: requ... | Vitest: call forceResolveRound as a member who is  |
| F4-01 | P1 | L | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:284-622` | forceResolveRound and triggerPhasePayday implement core auction-resolu... | Move the entire resolution algorithm into a SECURI... | Integration test: open auction with 2 competing bi |
| F4-ARCH-01 | P1 | L | B | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/actions.ts:368-622` | forceResolveRound is the entire round-resolution business logic implem... | Move the whole resolution into a single SECURITY D... | Integration test: concurrent forceResolveRound cal |
| F4-ARCH-02 | P1 | L | A | `apps/web/app/(game)/league/[leagueId]/auction/page.tsx:368-649` | Massive demo/real path duplication. renderDemoAuction (368-649) is a n... | Extract the data-loading + view-model building int... | Unit-test the extracted loader once with a fixture |
| F6-01 | P1 | L | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:368-622` | forceResolveRound is a full TypeScript port of the Python auction reso... | Move the entire resolution flow into a SECURITY DE... | Call forceResolveRound as an authenticated NON-com |
| BR-02 | P1 | L | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:368-622` | forceResolveRound re-implements the entire auction-resolution business... | Port resolve logic into a single SECURITY DEFINER ... | pytest/SQL: simulate a contract-insert failure mid |
| F4-06 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/rider-dialog.tsx:82-93` | The bid dialog's client-side validity check (isValid) uses budgetAfter... | Thread phaseConfirmed from the auction detail page... | Mount RiderDialog with phaseConfirmed=true and ass |
| F4-07 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/page.tsx:62-67` | The detail page surfaces cooldown via cooldownContracts (status='relea... | In market/page.tsx fetch released contracts with a... | Release a rider, load the market, assert the rider |
| F4-08 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/market/market-client.tsx:340-341` | The market computes each rider's min bid via calcMinSalary(r.pcs_point... | Use the stored monthly_salary as the displayed/val... | Construct a rider where monthly_salary != calcMinS |
| PERF-04 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/actions.ts:454-471` | N+1 query pattern inside `forceResolveRound`: for every rider in `byRi... | Hoist both fetches out of the loop: before the `fo... | Write a unit test that mocks the admin Supabase cl |
| PERF-05 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/actions.ts:307-334` | `triggerPhasePayday` fetches `league_members` and `teams` sequentially... | Merge the two initial fetches into a `Promise.all`... | Mock `admin.rpc` and assert that for N teams, all  |
| PERF-06 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/market/page.tsx:84-88` | Contracts query is a sequential waterfall: `leagueContracts` is fetche... | Pass `leagueId` directly to the contracts query fi... | Count Supabase calls made during `MarketPage` rend |
| PERF-10 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/history/page.tsx:57-83` | The history page fetches all bids for all closed rounds in a single un... | Add `.limit(1000)` as an explicit guard and a comm... | Write a test that seeds 1001 bid rows across close |
| PERF-11 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/results/page.tsx:31-44` | `select('*')` is used on the `auctions` table (line 32). This fetches ... | Replace `select('*')` with an explicit column list... | Static analysis / grep: assert no `select('*')` ex |
| DS-01 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/rider-dialog.tsx:220,227,245` | Inconsistent danger token: `--status-danger` is used at lines 220, 227... | Pick one canonical token. The design system tokens... | grep for `--status-danger` and `--status-success`  |
| DS-02 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/rider-table.tsx:80,90` | The two `<select>` filter controls use bare `rounded-md` (Tailwind def... | Replace `rounded-md` with `rounded-[var(--radius-m... | Visual regression snapshot of the rider-table filt |
| DS-03 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/rider-dialog.tsx:154,158` | The rider photo `<img>` and its fallback `<div>` both use bare `rounde... | Change `rounded-md` on the photo container (lines ... | Visual snapshot of the rider dialog header photo. |
| DS-04 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/market/market-client.tsx:350,559` | Two interactive elements use bare `rounded-md` instead of `rounded-[va... | Replace `rounded-md` with `rounded-[var(--radius-m... | grep asserting no bare `rounded-md` on interactive |
| DS-05 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/history/page.tsx:117,152,194,300,333,369` | All section-header divs and the search input use bare `rounded-lg` (Ta... | Replace `rounded-lg` with `rounded-[var(--radius-l... | grep asserting no bare `rounded-lg` in the auction |
| DS-07 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/history/page.tsx:176,355` | Money amounts in the bid list use `font-mono` without `tabular-nums` (... | Add `tabular-nums` to the `<span className="font-m... | Visual regression snapshot of bid list; or grep as |
| DS-09 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/rider-table.tsx:38-45` | The `SPECIALTY_LABELS` map at lines 38-45 maps `time_trialist` to `'CL... | Change `time_trialist: 'CLM'` to `time_trialist: '... | Render the rider table with a time_trialist rider; |
| DS-10 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/layout.tsx:12` | The regex used to hide sub-tabs on detail routes is `/\/auction\/(roun... | Use a more specific UUID regex: `/\/auction\/(roun... | Unit test: call the regex with `/auction/rounds/`, |
| DS-11 | P2 | S | B | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/auction-client.tsx:97-110` | The 'Cancel' button for a bid at line 97-110 calls `cancelBid(bid.id, ... | Lift `const cancelBidSafe = useDemoSafeAction(canc... | In a demo-context provider, render AuctionClient w |
| F4-ARCH-05 | P2 | S | B | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/page.tsx:87-92` | currentRound is computed by floor((now - opens_at)/86400000)+1 clamped... | Derive the round number from the auction row itsel... | Given an auction opened 3 days after opens_at, ass |
| F4-ARCH-06 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/auction-client.tsx:101` | cancelBid(bid.id, auctionId) is invoked on the inline 'Cancel' button ... | Route the inline cancel through a handler that awa... | Render AuctionClient, click Cancel, mock cancelBid |
| F4-ARCH-08 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/market/page.tsx:61-80` | The riders query for the market is bounded by .lte('pcs_rank', 600).li... | Add explicit pagination (or a documented assertion... | Seed >1000 active contracts across a league; asser |
| F4-ARCH-09 | P2 | S | B | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/page.tsx:61-67` | Cross-surface RLS/scoping inconsistency in the contracts queries. The ... | Centralize 'riders unavailable in league L' into o... | Add a rider signed in another league but ranked in |
| F6-06 | P2 | S | B | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/actions.ts:40-85` | cancelBid implements ownership verification (bid -> team -> user_id), ... | Wrap in a cancel_bid SECURITY DEFINER RPC that re-... | Race a cancelBid against the round closing and ass |
| TD-006 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/[auctionId]/actions.ts:31, 83` | revalidatePath('/league') and revalidatePath('/league') (backtick vari... | Pass the leagueId and auctionId (already available... | Mock revalidatePath, call placeBid with known IDs, |
| TD-007 | P2 | S | A | `apps/web/app/(game)/league/[leagueId]/auction/market/actions.ts:62` | setRoundDates (the older action) hard-codes the UTC offset as `+01:00`... | Replace the hard-coded `T23:59:59+01:00` with a ca... | Call setRoundDates with a date in July; assert the |
| F4-09 | P2 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:243-274` | validateRound's consensus auto-resolve is racy. It counts round_valida... | Make the consensus check + resolve atomic inside a... | Simulate two members validating concurrently for t |
| PERF-08 | P2 | M | A | `apps/web/app/(game)/league/[leagueId]/auction/market/market-client.tsx:185-216` | `filteredRiders` useMemo (line 185) runs a full `.filter()` + conditio... | Debounce the search input state update (150-200ms)... | Use React DevTools Profiler to measure render time |
| PERF-09 | P2 | M | A | `apps/web/app/(game)/league/[leagueId]/auction/market/market-client.tsx:340-396` | `renderRiderRight` is defined as a plain function inside the component... | Memoize `renderRiderRight` with `useCallback` and ... | Add a React Testing Library test that changes one  |
| PERF-12 | P2 | M | A | `apps/web/app/(game)/league/[leagueId]/auction/status/page.tsx:87-151` | The status page issues 5 sequential-then-parallel query phases for a l... | Restructure into two phases: Phase 1 `Promise.all(... | Instrument with timing logs in staging; assert tot |
| DS-08 | P2 | M | A | `apps/web/app/(game)/league/[leagueId]/auction/history/page.tsx:109-119` | The search input in the History page is a server-rendered static eleme... | Either extract the search box and bid list into a ... | Render the history page with 3 bids for rider 'Pog |
| DS-12 | P2 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/market/market-client.tsx:282-284` | `pendingBids` is defined as bids NOT in `savedDraftIds` (line 282-283)... | Redefine `pendingBids` to also include saved draft... | Render MarketClient with one initialDraftBid (ride |
| F4-ARCH-04 | P2 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:243-274` | validateRound does a consensus check in TS after the validate_round RP... | Fold the consensus check and conditional resolve i... | Concurrency test: N members validating in parallel |
| F4-ARCH-07 | P2 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:284-334` | triggerPhasePayday iterates league members and calls confirm_phase_set... | Make the payday cascade part of the same atomic re... | Run the resolve path twice for the same final roun |
| F6-05 | P2 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:66-213` | addDraft, removeDraft and updateDraftAmount carry business logic in TS... | Either move min-salary enforcement into a draft_bi... | Insert a draft_bid below calcMinSalary directly (o |
| F6-07 | P2 | M | B | `apps/web/app/(game)/league/[leagueId]/auction/rounds/actions.ts:57-188` | updateRoundDates and createNextPhaseAuctions perform commissioner auth... | Move commissioner gating and the auctions writes i... | Call createNextPhaseAuctions as a non-commissioner |
| PERF-07 | P2 | L | A | `apps/web/app/(game)/league/[leagueId]/auction/market/page.tsx:65-75` | The riders query fetches up to 600 rows with `.limit(600)` (line 74), ... | Add `export const revalidate = 3600` (or appropria... | Add a Playwright test that navigates to the market |
| SEC-F6-2 | P2 | L | B | `apps/web/app/(game)/league/[leagueId]/auction/actions.ts:284-622` | CLAUDE.md mandates the pattern 'Zod validation -> supabase.rpc(...) ->... | Port the resolution logic into a single SECURITY D... | After porting, a regression test asserting forceRe |