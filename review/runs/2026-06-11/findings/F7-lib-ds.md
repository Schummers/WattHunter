# F7-lib-ds — Findings (30)

| ID | Sev | Eff | Lane | file:line | Issue | Fix | Test idea |
|---|---|---|---|---|---|---|---|
| F7-DS-001 | P1 | S | A | `apps/web/components/ui/select.tsx:97` | SelectLabel uses the hardcoded Tailwind utility `text-xs` instead of a... | Replace `text-xs` with `text-[length:var(--type-ca... | Add a snapshot or a classname assertion in the exi |
| F7-DS-002 | P1 | S | A | `apps/web/components/ui/input.tsx:11` | `Input` applies `text-base` on mobile (all viewports below `md:`), the... | Replace `text-base md:text-[length:var(--type-body... | Write a classname test for both `<Input />` and `< |
| F7-DS-007 | P1 | S | A | `apps/web/lib/race-feed-helpers.ts:79-97` | `formatRaceDateLabel` and `formatRound1DateLabel` output French month ... | Replace `FRENCH_MONTHS_SHORT` and `FRENCH_LONG_MON... | Update `__tests__/race-feed-helpers.test.ts:126` t |
| F7-01 | P1 | S | A | `apps/web/lib/gt-stages.ts:149-158` | Duplicate `isCutoffPassedCET` implementation. The exact same 10-line f... | Extract `isCutoffPassedCET` into a shared module (... | Import the extracted function and assert `isCutoff |
| F7-02 | P1 | S | A | `apps/web/lib/race-feed-helpers.ts:75-77` | Duplicate `formatXp` function. A second `formatXp` exists here returni... | Rename `race-feed-helpers.ts:formatXp` to `formatX... | Add a test asserting `formatXpDelta(5)` returns `' |
| F7-03 | P1 | S | A | `apps/web/lib/sponsors.ts:58-64` | `formatBudget` has a dead branch. For values `>= 1_000_000`, the terna... | Change the fractional branch to `${m.toFixed(2).re... | Unit test `formatBudget(1_250_000)` = `'1.25M'`, ` |
| F7-12 | P1 | S | A | `apps/web/lib/gt-stage-schedule.ts:56-59` | `getReplaceWindowClosesAt` hard-codes the UTC offset as `+02:00` (CEST... | Compute end-of-day in `Europe/Paris` using `Intl.D... | Assert `getReplaceWindowClosesAt('giro-d-italia',  |
| F7-DS-01 | P1 | M | B | `apps/web/lib/league-creation.ts:18-100` | createLeagueWithTeam performs four sequential cross-table mutations (l... | Move the whole create-league-with-team flow into a... | Integration test: force the league_members insert  |
| F7-DS-02 | P1 | M | B | `apps/web/lib/get-race-feed-data.ts:133-158` | The XP, sponsor_bonus and sponsor_goal_completions fetches use unpagin... | Paginate these GT-wide fetches with a _fetch_all-e... | Mock the supabase client to return exactly 1000 ro |
| F2-ARCH-02 | P1 | L | B | `apps/web/lib/league-creation.ts:18-100:18-100` | League creation performs a multi-table write as a sequence of independ... | Move the whole create sequence into a SECURITY DEF... | Cannot be proven by a TS unit test (atomicity/RLS) |
| F2-ARCH-06 | P2 | S | A | `apps/web/lib/league-creation.ts:48:48` | max_players: 20 is a magic literal hardcoded in three independent crea... | Hoist max_players to a single named constant (e.g.... | Grep/lint assertion that no literal `max_players:  |
| PERF-05 | P2 | S | A | `apps/web/lib/rider-detail-data.ts:248-283` | Two sequential `await` calls follow the parallel `Promise.all` block t... | Merge `draft_bids` into the first `Promise.all` al... | Spy on the supabase client. Call `fetchRiderDetail |
| F7-DS-003 | P2 | S | A | `apps/web/components/ui/card.tsx:35` | `CardTitle` renders with only `leading-none font-semibold` — no typogr... | Add `text-[length:var(--type-section)]` to `CardTi... | Snapshot test for `CardTitle` verifying it include |
| F7-DS-004 | P2 | S | A | `apps/web/lib/sponsors.ts:61` | `formatBudget` contains a dead branch: `m === Math.floor(m) ? \`${m}M\... | Simplify to a single expression: `const str = (mon... | Unit test: `formatBudget(1_000_000)` → `'1M'`, `fo |
| F7-DS-005 | P2 | S | A | `apps/web/lib/gt-stages.ts:149-158` | `isCutoffPassedCET` is copy-pasted verbatim in both `apps/web/lib/gt-s... | Extract `isCutoffPassedCET` into a shared utility,... | The existing test suites for `gt-stages` and `get- |
| F5-D7 | P2 | S | B | `apps/web/lib/rider-detail-data.ts:158-166` | `raceMeta` is keyed by `race_slug` and keeps only the FIRST row per sl... | Add a deterministic `.order(...)` (e.g. by a stabl... | Provide two `race_results` rows for the same (ride |
| F7-DS-04 | P2 | S | B | `apps/web/lib/phases.ts:37-59` | Two different phase-boundary definitions coexist. getCurrentPhase (lin... | Derive getPhaseRange.end from the same next-phase-... | For every phase, assert getPhaseRange(phase).end m |
| F7-DS-05 | P2 | S | A | `apps/web/lib/strategies.ts:42-44` | getMaxActiveStrategies hardcodes the level->max-active mapping (level>... | Make getMaxActiveStrategies read getLevelByNumber(... | Parametrized test: for levels 1..8 assert getMaxAc |
| F7-DS-06 | P2 | S | A | `apps/web/lib/boost.ts:45-48` | riderMatchesStrategy has a 'road_warriors' case (lines 45-48), but no ... | Remove the road_warriors case (and the rwAge local... | Assert every case label in riderMatchesStrategy's  |
| F7-DS-07 | P2 | S | A | `apps/web/lib/sponsors.ts:58-64` | formatBudget has a no-op conditional: when monthly >= 1_000_000 it ret... | Collapse to a single `return `${m}M`` or implement... | Assert formatBudget(1_250_000) === '1.25M' and for |
| F7-04 | P2 | S | A | `apps/web/lib/boost.ts:43-49` | Dead strategy case `road_warriors` handled in `riderMatchesStrategy` (... | Either remove the `road_warriors` case from `boost... | Add a test asserting that every `case` in `riderMa |
| F7-06 | P2 | S | A | `apps/web/lib/phases.ts:37-52` | `getCurrentPhase` has no coverage for dates outside the season (before... | Return an explicit sentinel (or `null`) for out-of... | Test `getCurrentPhase(new Date('2026-12-01'))` and |
| F7-07 | P2 | S | A | `apps/web/lib/strategies.ts:42-44` | `getMaxActiveStrategies` encodes level thresholds (1, 3, 7) as magic n... | Replace the ternary with `getLevelByNumber(level).... | The existing test suite in `strategies.test.ts` al |
| F7-08 | P2 | S | A | `apps/web/lib/format.ts:53-61` | `SALARY_COEFFICIENT`, `SALARY_FLOOR`, and `calcMinSalary` are defined ... | Move `SALARY_COEFFICIENT`, `SALARY_FLOOR`, and `ca... | Move the functions; existing callers that import f |
| F7-10 | P2 | S | A | `apps/web/lib/help-table.tsx:27-28` | `key={i}` used for both row index and `key={j}` for cell index in the ... | Compose a stable key from row content or pass an o... | No runtime impact; a lint rule (`react/no-array-in |
| F7-DS-03 | P2 | M | B | `apps/web/lib/rider-detail-data.ts:117-122` | fetchRiderDetailData reads rider_xp_daily and sponsor_bonuses (lines 1... | Apply the same paginated _fetch_all pattern to the... | Mock 1000+ rider_xp_daily rows for one rider in on |
| F7-DS-08 | P2 | M | A | `apps/web/lib/levels.ts:1-10` | The LEVELS constant mixes pure game-mechanics data (xp, slots, poolMin... | Split presentation from data: keep only mechanical... | Assert that the sponsor label rendered for a level |
| F7-05 | P2 | M | B | `apps/web/lib/supabase/get-open-auction.ts:18-38` | `getOpenAuction` performs a direct `UPDATE auctions SET status='open'`... | Convert the lazy-open mutation to an RPC (`open_du... | Write an integration test asserting that two concu |
| F7-09 | P2 | M | A | `apps/web/lib/get-race-feed-data.ts:87-103` | GT slug and name maps are duplicated inline twice within the same file... | Replace both inline map literals with imports of `... | Compile-time only — TypeScript will catch any key  |
| F7-DS-09 | P2 | L | A | `apps/web/lib/rider-detail-data.ts:77-509` | fetchRiderDetailData is a 430-line god-function that interleaves rider... | Decompose into context-specific composables (e.g. ... | After extraction, unit-test each composable in iso |