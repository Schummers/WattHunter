# B5-sync-scraping — Findings (38)

| ID | Sev | Eff | Lane | file:line | Issue | Fix | Test idea |
|---|---|---|---|---|---|---|---|
| PERF-12 | P0 | M | B | `services/pcs-sync/sync_race.py:217-219, 293-295` | import_gc_results and import_final_classifications each perform an unp... | Apply the _fetch_all pattern already established i... | Seed a mock supabase with 1001 rider rows; assert  |
| TD-01 | P0 | M | B | `services/pcs-sync/sync_race.py:139, 217, 292, 353, 589, 654` | Seven unpaginated `riders` table fetches across `import_race_results`,... | Replace every `supabase.table('riders').select('id... | Add a test with a mocked `_PaginatingQuery` (same  |
| PERF-02 | P1 | S | A | `services/pcs-sync/sync_race.py:436-449` | update_global_ranking issues two separate UPDATE statements per rider ... | Merge ever_in_pool=True into the first update payl... | Mock supabase.table('riders').update(...).eq(...). |
| SEC-B5-02 | P1 | S | A | `services/pcs-sync/photo_storage.py:99-105` | Storage path injection / object overwrite. upload_rider_photo() builds... | Sanitize pcs_slug into a safe object key: strip to... | Call upload_rider_photo with pcs_slug='../../evil' |
| TD-02 | P1 | S | B | `services/pcs-sync/sync_race.py:388-390` | Inside `update_global_ranking`, any ranking entry where `pcs_points ==... | Separate the 'skip salary update for zero-point ri... | Unit test: build a rider_map with a rider whose po |
| TD-03 | P1 | S | A | `services/pcs-sync/sync_race.py:436-449` | Inside `update_global_ranking`, for each existing rider two separate `... | Add `'ever_in_pool': True` to the dict on line 438... | Mock the supabase client and assert that for a mat |
| TD-04 | P1 | S | A | `services/pcs-sync/run_pipeline.py:534` | `if True:` is used as an unconditional block guard for the GC import i... | Remove the `if True:` wrapper entirely and dedent ... | Static-analysis / AST check: assert no `if True:`  |
| TD-06 | P1 | S | A | `services/pcs-sync/sync_race.py:530` | Inside `import_season_rankings`, the riders lookup (`supabase.table('r... | Move the `riders_resp` fetch and `rider_map` const... | Mock supabase and assert `.table('riders').select( |
| PERF-01 | P1 | M | A | `services/pcs-sync/sync_race.py:139-141, 217-219, 293-295, 353-355, 589-591, 655-657` | riders table is fetched in full (select id, pcs_slug) inside every imp... | Build the rider_map once at the call site that own... | Unit-test import_race_results with a pre-built rid |
| PERF-03 | P1 | M | A | `services/pcs-sync/sync_race.py:465-529` | In the full-manual stage-import path (no target_stage), each of potent... | Reuse a single context+page for all sequential sta... | Spy on browser.new_context() in a test that drives |
| PERF-04 | P1 | M | A | `services/pcs-sync/sync_race.py:474-528` | In full-stage import mode, supabase.table('race_results').upsert() is ... | Batch rows into a single upsert call per function ... | Patch supabase.table and assert .upsert() is calle |
| ARCH-B5-01 | P1 | M | B | `services/pcs-sync/sync_race.py:139, 217, 292, 353, 530, 589, 654; sync.py:185 (rider lookup maps)` | The rider lookup map (`pcs_slug -> rider_id`) is built with a bare `su... | Route every `riders` lookup-map build through `db_... | Seed a fake Supabase that returns 1500 rider rows  |
| ARCH-B5-03 | P1 | M | B | `services/pcs-sync/run_pipeline.py:603-708 (run_post_race), 371-600 (_import_single_race)` | run_post_race orchestrates a write-heavy fan-out — import → calculate_... | Wrap the post-race write phase in an orchestrator-... | Run the post-race chain twice on the same stage ag |
| SEC-B5-01 | P1 | M | B | `services/pcs-sync/photo_storage.py:36-40, 48-92, 123` | SSRF / credentialed-fetch exfiltration from the privileged scraper con... | Before fetching, assert the resolved URL's host is... | Unit-test resolve_pcs_image_url/self_host_photo wi |
| TD-05 | P1 | M | A | `services/pcs-sync/enrich.py:430-503, 506-583` | `enrich_riders` (lines 430-503) and `enrich_missing_riders` (lines 506... | Extract a shared `_enrich_rider_list(supabase, rid... | Verify that `enrich_riders` and `enrich_missing_ri |
| PERF-05 | P2 | S | A | `services/pcs-sync/sync_race.py:478-488` | In update_global_ranking, riders dropped from the top 600 are updated ... | Use a single bulk update: supabase.table('riders')... | Unit-test with a dropped_ids set of 10 elements; a |
| PERF-06 | P2 | S | A | `services/pcs-sync/sync.py:183-188` | sync_top500 calls supabase.table('riders').upsert() once per rider ins... | Collect all rider_data dicts for the page into a l... | Mock supabase and assert .upsert() is called once  |
| PERF-07 | P2 | S | A | `services/pcs-sync/sync_race.py:528-529` | import_season_rankings rebuilds the rider_map via a full riders table ... | Move the riders_resp / rider_map construction to b... | Spy on supabase.table('riders').select().execute() |
| PERF-09 | P2 | S | A | `services/pcs-sync/sync.py:83-109` | fetch_html always starts with asyncio.sleep(delay) where delay default... | Make the pre-navigation sleep conditional or reduc... | Patch asyncio.sleep and page.wait_for_timeout; cal |
| PERF-11 | P2 | S | A | `services/pcs-sync/run_pipeline.py:159-183` | load_calendar() and lookup_race() read and parse wt_calendar_2026.json... | Apply @functools.lru_cache(maxsize=1) to load_cale... | Call load_calendar() twice and assert the underlyi |
| ARCH-B5-05 | P2 | S | B | `services/pcs-sync/browser_session.py:100-119, 121-127, 244-245` | The nodriver backend's `new_context` is a no-op isolation primitive: `... | Document the nodriver no-isolation semantics at ev... | Assert _NodriverContext instances created twice fr |
| ARCH-B5-07 | P2 | S | B | `services/pcs-sync/run_pipeline.py:400-403, 428-430, 449-453, 521-522, 550-551` | Import failures are swallowed and reduced to a print: every per-stage ... | Distinguish 'expected empty' (no results yet) from... | Simulate import_gc_results raising a Cloudflare Ru |
| ARCH-B5-08 | P2 | S | B | `services/pcs-sync/enrich.py:385-421` | enrich_single_rider writes into race_results (step 5, season_results) ... | Have enrich write season_results into a separate s... | Insert a season_results row via enrich for a WT-ca |
| ARCH-B5-09 | P2 | S | A | `services/pcs-sync/run_pipeline.py:159-162, 171, 240, 74-90` | load_calendar() re-reads and re-parses wt_calendar_2026.json from disk... | Add a single `@functools.lru_cache` accessor for t... | Patch open() to count reads; call lookup_race + ra |
| ARCH-B5-10 | P2 | S | A | `services/pcs-sync/run_pipeline.py:64-101 (GT/squad helpers), 678-683` | The Grand-Tour / stage-race classification logic is duplicated between... | Promote the race-classification helpers (GT prefix... | Import the shared classifier from both the pipelin |
| SEC-B5-03 | P2 | S | A | `services/pcs-sync/photo_storage.py:80-92, 99-105` | Unvalidated content stored as public image. The content-type used to c... | Validate the decoded bytes against known image mag... | Feed download_image_via_page-style bytes that are  |
| SEC-B5-06 | P2 | S | A | `services/pcs-sync/enrich.py:161-168, 236` | Scraped <img src> is propagated as a trusted URL without host validati... | Constrain the accepted src to a relative PCS path ... | Parse a rider HTML fixture containing <img src='ht |
| TD-07 | P2 | S | A | `services/pcs-sync/sync.py:63-72` | The Cloudflare marker list `CLOUDFLARE_MARKERS` in `sync.py` (lines 63... | Move the canonical marker collection to `browser_s... | Assert at import time (or in a test) that `sync.CL |
| TD-08 | P2 | S | A | `services/pcs-sync/sync.py:182, enrich.py:319, sync_race.py:411` | `datetime.utcnow().isoformat()` is used in three files to set `last_sy... | Add `from datetime import datetime, timezone` in e... | Run `python -W error::DeprecationWarning -c 'from  |
| TD-09 | P2 | S | A | `services/pcs-sync/sync_race.py:697` | `import re` is duplicated at the module level (line 9) and again as a ... | Remove the `import re` statement at line 697 insid... | Linting: `flake8 --select=F811 sync_race.py` shoul |
| TD-10 | P2 | S | A | `services/pcs-sync/run_pipeline.py:177-178` | Inside `lookup_race`, `import re` is a local import executed on every ... | Remove the `import re` at line 177 inside `lookup_... | Linting: `flake8 --select=F811 run_pipeline.py` sh |
| PERF-08 | P2 | M | A | `services/pcs-sync/enrich.py:324-353` | enrich_single_rider issues one supabase upsert per team entry (rider_t... | Batch the rider_teams rows and rider_season_rankin... | Pass a rider with 5 team history entries and 5 sea |
| PERF-10 | P2 | M | A | `services/pcs-sync/photo_storage.py:56-92` | download_image_via_page transfers the image from the browser to Python... | Keep the approach as-is for correctness (it is the... | Call self_host_photo with a photo_url that passes  |
| ARCH-B5-02 | P2 | M | A | `services/pcs-sync/sync_race.py:139-142, 217-220, 292-295, 353-356, 530-533, 589-592, 654-657` | The 4-line rider-map construction (`riders_resp = ...select('id, pcs_s... | Add `build_rider_map(supabase) -> dict[str,str]` t... | Grep test asserting no inline `{r['pcs_slug']: r[' |
| ARCH-B5-06 | P2 | M | B | `services/pcs-sync/run_pipeline.py:276-279 (stage_num computed), 470-471 (target_stage indexes get_stage_urls)` | Auto-mode stage detection has two independent, potentially inconsisten... | Validate the calendar-derived stage_num against th... | Given a calendar with a rest day that shifts numbe |
| SEC-B5-04 | P2 | M | A | `services/pcs-sync/run_pipeline.py:839-852, 889-898, 1024-1057, 1096-1099` | Privileged treasury/eligibility mutations driven by unvalidated CLI in... | Validate args in run_pipeline before dispatch: ass... | Invoke resolve-gt-rescue with --league 'not-a-uuid |
| SEC-B5-05 | P2 | M | B | `services/pcs-sync/sync.py:17-18, 52-53` | service_role key held process-wide with no least-privilege scoping. SU... | Where feasible, run the scraper against a dedicate... | N/A (architectural / least-privilege); verify the  |
| ARCH-B5-04 | P2 | L | B | `services/pcs-sync/run_pipeline.py:371-600` | _import_single_race is a 230-line god-function with four near-duplicat... | Extract the repeated unit into `_import_stage_and_... | Parametrized test feeding a /gc slug, a /stage-N s |